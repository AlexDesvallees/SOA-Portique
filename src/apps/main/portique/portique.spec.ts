// Alex - Fichier généré automatiquement par le CLI...
import { PortiqueDTO } from './portique.entity';

describe('Portique', () => {
  it('should be defined', () => {
    expect(new PortiqueDTO()).toBeDefined();
  });
});
