{
    "procedures": [
        {
            "name": "Lect_Personne",
            "request": "SELECT nom, prenom, status_id, compte_id, isBlocked FROM Personne WHERE personne_id = ?"
        },
        {
            "name": "Lect_Liste_Personne",
            "request": "SELECT nom, prenom, status_id, compte_id, isBlocked FROM Personne"
        }
    ]
}